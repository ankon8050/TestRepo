<%= form_for(@mark_sheet) do |f| %>
  <% if @mark_sheet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mark_sheet.errors.count, "error") %> prohibited this mark_sheet from being saved:</h2>

      <ul>
      <% @mark_sheet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :subject_name %><br>
    <%= f.text_field :subject_name %>
  </div>
  <div class="field">
    <%= f.label :total_number %><br>
    <%= f.text_field :total_number %>
  </div>
  <div class="field">
    <%= f.label :student_id %><br>
    <%= f.number_field :student_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
